use bevy::prelude::*;

/// # Bevy Plugin for rendering Text(No format text)

pub fn setup_txt_render_system(
    mut commands: Commands,
    asset_server: Res<AssetServer>,
) {
}

pub fn load_txt_from_local_file_system() {}
